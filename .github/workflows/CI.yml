name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y g++ make libgtest-dev cmake

    - name: Build Tests
      run: make test

    - name: Check if test binary exists
      run: ls -lh test-Lab1

    - name: Upload Test Binary
      uses: actions/upload-artifact@v4
      with:
        name: test-binary
        path: test-Lab1

  run_tests:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Download Test Binary
      uses: actions/download-artifact@v4
      with:
        name: test-binary
        path: ./

    - name: Make binary executable
      run: chmod +x ./test-Lab1

    - name: Run Tests
      run: ./test-Lab1
